export type EventType = {
    id: string;
    categoryId: string;
    longitude: string;
    latitude: string;
    start: string;
    end: string;
    title: string;
    note: string;
    metadata: string;
    link: string;
    formattedDate: string;
}
